import { Button } from 'components/atoms';

export default function PreGameStage() {
    return (
        <div className="PreGameStage">
            <span>Ready to start playing?</span>
            <Button>Begin round!</Button>
        </div>
    );
}
